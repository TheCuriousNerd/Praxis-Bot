version: '3.7'
services:
  standalone_lights:
    image: standalone_lights
    ports:
      - 42042:42042
    environment:
      - ISDOCKER=cat
    networks:
      - praxisbot
    volumes:
      - "../logs:/Praxis/logs"
      - "../credentials:/Praxis/credentials"
networks:
  praxisbot:
    name: praxisbot